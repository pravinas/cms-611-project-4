using UnityEngine;
using System.Collections;

public class GlowImage : MonoBehaviour {
	/*
	private GameObject img;

	// Use this for initialization
	void Start () {
		img = this.gameObject;
		print (img.ToString);
		img.transform.position.Set (img.transform.position.x, img.transform.position.y, - 100);
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnMouseEnter(){
		img.transform.position.Set (img.transform.position.x, img.transform.position.y, 1);
	}

	void OnMouseExit(){
		img.transform.position.Set (img.transform.position.x, img.transform.position.y, - 1);
	}*/
}
